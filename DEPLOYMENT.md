cat > DEPLOYMENT.md << 'EOF'
# 🚀 Deployment Guide

## Local Development
1. Install dependencies: `npm install`
2. Configure .env file
3. Start: `npm run dev`

## Docker Deployment
```bash
docker-compose up -d
